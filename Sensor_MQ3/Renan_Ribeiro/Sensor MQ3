// C++ code
//
void setup()
{
  pinMode(A5, INPUT);
  pinMode(5, OUTPUT);
}

void loop()
{
  if (analogRead(A5) > 500) {
    digitalWrite(5, HIGH);
  } else {
    digitalWrite(5, LOW);
  }
  delay(10); // Delay a little bit to improve simulation performance
}

